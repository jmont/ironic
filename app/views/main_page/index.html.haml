.row
  #scrollView.span3.conf.offset1
    - for c in @confessions
      .wrapper{:data => {:id => "#{c.id.to_s}"}}
        =c.id.to_s
      .confession{:data => {:id => "#{c.id.to_s}"}}= c.txt
    = will_paginate @confessions

  #mainDetailView.span8
